# The extension runs its source directly (no bundle step). Ship extension.js,
# src/, and the jimp runtime dependency; exclude dev-only and heavy files.
.env
.env.*
.vscode/**
test/**
samples/**
scripts/**
dist/**
install.sh
PUBLISHING.md
.gitignore
.git/**
.vscodeignore
scratchpad/**
**/*.map
*.vsix
